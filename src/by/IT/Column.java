/////package by.IT;


////антация описывает имя ключа в файле, на которое должно ссылатся поле класса для получения соот значени

/////import java.util.Map;
import java.lang.annotation.ElementType;
import java.lang.annotation.Retention;
import java.lang.annotation.RetentionPolicy;
import java.lang.annotation.Target;
import java.util.regex.Matcher;
import java.util.regex.Pattern;

////public @interface Column {
   //// String name();

 //// Matcher matcher = Pattern.compile(REGEX).matcher(boolean);
//////@Target(ElementType.FIELD)
////@Retention(RetentionPolicy.CLASS)
/////public @interface

 //// if (matcher.matches()){
  ////    book = new Boor.Book();
    ///  ///// получаем информацию осоответсвии названию полей класса
        ///// с названиями соответсвия
       //// Map<String, String> map = ColumnAnalyzer.analyzes(book);



     /// for (int i = 1; i < 6; i += 2){
       ///   String fieldKey = matcher.group(i);
         //// String fieldValue = matcher.group(i + 1);


      ///}
   /// }

////}
